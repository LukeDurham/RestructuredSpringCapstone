import { styled } from '@mui/system';
import { ProSidebar } from 'react-pro-sidebar';

export const StyledSideBar = styled(ProSidebar)({
  backgroundColor: '#1F1B24', // Your chosen background color
  width: '10%', // Adjust according to your preference
  minWidth: '125px', // Ensuring it doesn’t get too narrow on smaller screens
  height: '100vh',


  // Additional styling..
  // Add any additional styling here
});
